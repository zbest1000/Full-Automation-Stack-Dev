# Tailscale Configuration
TS_AUTHKEY=tskey-auth-xxxxxxxxxxxxx
TAILSCALE_HOSTNAME=automation-stack

# FlowFuse Domain Configuration
# FlowFuse requires a domain for Node-RED instance routing
# Use your Tailscale hostname with .ts.net suffix, or your own domain
# This domain is used to route requests to Node-RED instances created by FlowFuse
# Format: <instance-name>.<domain> (e.g., myflow.automation-stack.ts.net)
FLOWFUSE_DOMAIN=automation-stack.ts.net

# PostgreSQL
# Note: This PostgreSQL instance is shared by FlowFuse and MonsterMQ
# FlowFuse uses the database specified in POSTGRES_DB
# MonsterMQ uses a separate database 'monstermq' (created automatically)
POSTGRES_USER=flowfuse
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DB=flowfuse

# Grafana
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=your_secure_password

# InfluxDB
INFLUXDB_ADMIN_USER=admin
INFLUXDB_ADMIN_PASSWORD=your_secure_password
INFLUXDB_BUCKET=automation
INFLUXDB_ORG=automation
INFLUXDB_RETENTION=0

# HiveMQ
HIVEMQ_ALLOW_ANONYMOUS=true
HIVEMQ_USER=
HIVEMQ_PASSWORD=

# MonsterMQ Configuration
# Note: MonsterMQ uses config.yaml file, not environment variables
# See config/monstermq-config.yaml.example for configuration
